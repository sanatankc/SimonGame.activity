export default class PlaybackPosition {
    private totalTime;
    private labels;
    private offsets;
    constructor();
    parseLabel(name: any, offset: any): number;
    addLabel(name: any, offset: any): this;
    setLabel(name: any, offset: any): this;
    eraseLabel(name: any): this;
}
